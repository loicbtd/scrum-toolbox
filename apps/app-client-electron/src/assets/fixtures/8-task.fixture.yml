entity: task
items:
  task{1..20}:
    label: '{{lorem.words}}'
    description: '{{lorem.paragraph}}'
    status: '@task_status*'
    type: '@task_type*'
    project: '@project*'
    sprint: '@sprint*'
    capacity: '{{datatype.number(10)}}'
    users:
      - '@users*'
  task{20..40}:
    label: '{{lorem.words}}'
    description: '{{lorem.paragraph}}'
    status: '@task_status*'
    type: '@task_type*'
    project: '@project*'
    capacity: '{{datatype.number(10)}}'
